# Notebook Catalog

## Featured notebooks (portfolio-first)

Start here when evaluating this portfolio:

- `healthcare_analysis.ipynb` — end-to-end healthcare explainability and calibration workflow.
- `life_science_medical_imaging_triage.ipynb` — full medical-image triage example with CNN baselines, Grad-CAM, and fairness audit.
- `insurance_data_science.ipynb` — pricing and segmentation benchmark with governance and drift checks.
- `finance_credit_risk_stress_testing.ipynb` — stress-tested credit risk and default-threshold decision framework.
- `genai_rag_pipeline.ipynb` — production-pattern RAG stack with tracing and traceability controls.
- `energy_load_probabilistic_forecasting.ipynb` — probabilistic forecasting with uncertainty calibration.
- `mlops_mlflow_drift_lifecycle.ipynb` — MLflow-based lifecycle and drift-triggered promotion flow.

## Archive / reference notebooks

All remaining notebooks are intentionally kept as reference material for reproducibility and breadth:

- They remain accessible in this folder and should be treated as supplemental.
- Use this list for deeper exploration after reviewing featured notebooks.

Quick index of notebooks under `notebooks/`, with suggested usage and expected runtime on a standard laptop CPU.

| Notebook | Domain | Level | Primary Use Case | Est. Runtime |
| --- | --- | --- | --- | --- |
| `healthcare_analysis.ipynb` | Life Science / Healthcare | Advanced | End-to-end healthcare modeling, explainability, calibration, drift/fairness checks | 8-15 min |
| `life_science_clinical_response_safety.ipynb` | Life Science | Core | Dual-endpoint benchmarking, calibration, subgroup consistency checks, and benefit-risk dose policy optimization | 4-7 min |
| `life_science_survival_censoring_analysis.ipynb` | Life Science | Advanced | Right-censored survival analysis with KM/RMST, log-rank, Cox PH, and discrete-time hazard trajectory modeling | 6-10 min |
| `life_science_medical_imaging_triage.ipynb` | Life Science / Computer Vision | Advanced | Pneumonia detection on chest X-rays (PneumoniaMNIST, 28×28, 5,856 images): logistic-regression baseline + small CNN from scratch + pre-trained ResNet-18 fine-tune; ROC/PR/AUPRC comparison, calibration (reliability, ECE, Brier, Platt recalibration), Grad-CAM overlays on correct and misclassified cases, fairness audit across simulated imaging-device subgroups with parity gate, clinical decision curve analysis (net benefit), asymmetric-cost operating-point selection (5:1 FN:FP), model card + weight persistence + inference-parity check. CPU-only. Requires `torchvision` + `medmnist` | 4-5 min |
| `insurance_data_science.ipynb` | Insurance | Advanced | Pricing/risk segmentation workflow extended with governance benchmarking, calibration economics, PSI drift, and subgroup stability monitoring | 8-14 min |
| `actuarial_pricing_and_reserving.ipynb` | Insurance | Core | Integrated actuarial pricing (frequency/severity) and reserving (chain-ladder + bootstrap) governance workflow | 5-9 min |
| `insurance_fraud_triage_optimization.ipynb` | Insurance | Core | Fraud triage benchmarking, calibration, capacity queue simulation, and economics-driven SIU policy optimization | 4-7 min |
| `insurance_reserving_triangle_chainladder.ipynb` | Insurance | Advanced | Chain-ladder reserving with triangle diagnostics, bootstrap uncertainty, risk-margin view, and governance metrics | 4-7 min |
| `finance_credit_risk_stress_testing.ipynb` | Finance | Core | Credit risk PD benchmarking, cost-aware thresholding, and multi-scenario stress expected-loss analysis | 4-7 min |
| `finance_market_risk_var_backtesting.ipynb` | Finance | Advanced | Multi-model VaR estimation with Kupiec/Christoffersen backtests, regime diagnostics, and stressed capital interpretation | 4-7 min |
| `causal_inference_policy_evaluation.ipynb` | Causal Inference | Advanced | Panel-data policy evaluation with known ground truth: Naive, OLS, PSM/IPTW, DiD, event study, Synthetic Control, IV (2SLS); estimators compared against a simulated true ATT | 2-4 min |
| `customer_retention_uplift_modeling.ipynb` | Causal ML / Customer Analytics | Advanced | Uplift / CATE modelling for a churn-retention campaign on a synthetic 10,000-customer corpus with known heterogeneous CATE across 5 segments (including sleeping-dogs where the offer backfires). Five hand-rolled meta-learners (S / T / X / DR-AIPW with 5-fold cross-fitting / R-learner via Robinson residualisation), CATE quality vs ground truth (Pearson r, Kendall τ, RMSE, segment-level recovery), uplift curves, Qini coefficient, AUUC, policy-value at top-k %, cost-aware targeting with an explicit break-even threshold, sleeping-dog-recall diagnostic, CATE calibration, permutation-importance attribution, model card + persistence + inference-parity check. CPU-only | ~30s |
| `ecommerce_recommender_two_tower_rerank.ipynb` | Recommender Systems | Advanced | Two-stage cascade recommender on a synthetic 2,000-user × 500-item e-commerce corpus: popularity + item-CF + MF (TruncatedSVD) baselines vs two-tower neural retrieval (BPR loss with uniform random negatives) + MLP cross-encoder reranker; MMR diversification; offline eval with Hit@k, NDCG@k, MAP@k, catalog coverage, intra-list diversity, Gini; cold-start subgroup eval; popularity-bias / long-tail diagnostics; counterfactual off-policy evaluation (IPS / SNIPS / DR) with a simulated logged policy; per-request latency breakdown under a serving-cascade SLO; feature-ablation explanations; model card + weight persistence + inference-parity check. CPU-only | ~1 min |
| `genai_rag_pipeline.ipynb` | GenAI / AI Engineering | Advanced | Production RAG pipeline reusing `src/genai/` primitives: 40-doc synthetic KB, recursive chunking, sentence-transformers embeddings, FAISS dense + BM25 + reciprocal-rank-fusion hybrid + cross-encoder reranker, HyDE and multi-query transforms, versioned prompt registry, Pydantic structured output with citation parsing, ingress PII/injection and egress hallucination guardrails, per-request tracing + cost/latency accounting. Emits `artifacts/genai/predictions.jsonl` for the eval notebook. Offline-reproducible via `FakeLLMClient`; `USE_LIVE_API` flag swaps in Anthropic or OpenAI | 1-2 min |
| `llm_rag_evaluation.ipynb` | GenAI / AI Engineering | Advanced | RAGAS-style RAG evaluation that consumes the pipeline's `predictions.jsonl`: retrieval metrics (Hit@k / MRR@10 / nDCG@5 / context-recall) with bootstrap 95% CIs, LLM-as-judge generation metrics (faithfulness, answer-relevance, context-precision) via versioned judge prompts, judge calibration vs structural oracle (Cohen's κ, Pearson r, Spearman ρ) with degenerate-case guard and synthetic-noise methodology demo, difficulty-stratified slicing, cost-quality Pareto, release-gate decision emitted as `eval_report.json` | 1 min |
| `genai_service_delivery.ipynb` | GenAI / AI Engineering | Advanced | Production delivery layer wrapping the same `RAGPipeline` behind a FastAPI service. Refuses to boot in production mode unless `eval_report.json` passed the gate. Pydantic schemas, bearer-token + tenant auth, per-tenant token-bucket rate limiting, retry with backoff + circuit breaker, append-only PII-redacted JSONL audit log, per-tenant P50/P95 latency + cost metrics, full TestClient e2e suite (401 / 403 / 200 / PII / injection / rate-limit), pre-deployment canary run with SLO gate (p95 / error / grounded), API-contract backward-compatibility check, K8s Deployment + HPA + PDB, GitHub Actions workflow with eval gate, and a consolidated `release_decision.json` (eval + canary + contract) | 1 min |
| `genai_dataops_vector_platform.ipynb` | GenAI / AI Engineering | Advanced | Operational sibling to the RAG pipeline. Multi-tenant 60-doc corpus with seeded near-duplicates; ingestion telemetry; persistent FAISS (save/reload/verify bit-identical results); `IndexFlatIP` vs `IndexIVFFlat` vs `IndexHNSWFlat` benchmark (build time / query latency / size / recall@5 vs Flat); union-find near-duplicate detection with tunable threshold; versioned incremental ingestion (v1 → v2 delta with parent manifest); metadata filtering + tenant-isolation audit; four policy-driven data-quality gates at ingestion; `vector_platform_manifest.json` with promote / block recommendation | 1 min |
| `maritime_illegal_fishing_detection.ipynb` | Geospatial / Conservation | Advanced | AIS vessel-track anomaly detection for MPA enforcement: simulated 60-vessel fleet with bad-actor routing, rule-based + supervised (HistGBC) + ten-detector unsupervised benchmark (IF / LOF / OC-SVM / ECOD / COPOD / MLP-AE / VAE / LSTM-AE / Transformer-AE / TCN-AE), precision@K patrol queue, and geospatial map of flagged tracks | 6-10 min |
| `fisheries_stock_assessment_bayesian.ipynb` | Fisheries Science / Bayesian | Advanced | Full stock-assessment + management pipeline: Schaefer dynamics, hand-rolled Metropolis-Hastings MCMC state-space, R-hat/ESS/PPC/retrospective/prior-sensitivity diagnostics, Kobe plot, HCR simulation testing, SST-dependent r climate extension, Schaefer-vs-Fox AIC comparison, decision-theoretic utility, full MSE with annual re-estimation | 4-6 min |
| `demographics_lee_carter_mortality_forecasting.ipynb` | Demographics / Actuarial | Advanced | Lee-Carter mortality forecasting with MCMC, life expectancy and annuity valuation, longevity SCR; plus extensions for parameter bootstrap, Renshaw-Haberman cohort, CBD, Li-Lee multi-population, COVID shock, and Solvency II QRT template | 5-7 min |
| `urban_demographics_service_planning.ipynb` | Demographics / Urban Planning | Advanced | Multi-district cohort-component population projection with stochastic Monte Carlo, service-demand translation (schools/healthcare/long-term care), capacity-gap analysis, and investment prioritisation for municipal capital planning | 3-5 min |
| `energy_load_probabilistic_forecasting.ipynb` | Energy / Time Series | Advanced | Hourly electricity-load forecasting on the **UCI Individual Household Power Consumption** dataset (~2M minute-level rows aggregated to hourly): rolling-origin backtest with four weekly cutoffs and **honest** champion-challenger framing — `seasonal_naive` / `ets` / `lightgbm` (point + 3 quantile heads) compared per fold, with **SARIMAX(2,0,1)(1,0,0,24) + Fourier weekly exog** as the calibrated-interval backbone. Probabilistic stack: split conformal on SARIMAX residuals + **Adaptive Conformal Inference** (Gibbs & Candès 2021) with online α update for non-stationary streams. Eval: pinball loss, **CRPS**, empirical coverage, **Winkler interval score**, MASE, with **Diebold-Mariano** HAC-variance test on squared-error differentials. Production hygiene: PSI drift gate on lag/rolling features, rolling-MAPE alert rule, persisted LightGBM champion (~13 MB joblib) with bit-identical inference-parity check, model card. CPU-only | 8-12 min |
| `streaming_sensor_anomaly_alert_fatigue.ipynb` | Streaming / Anomaly Detection | Advanced | Online univariate anomaly detection on **four NAB streams** (NYC taxi demand, factory IoT temperature, AWS EC2 CPU, ad-exchange CPC) with point-by-point streaming evaluation and **alert-fatigue economics**. Eight detectors with a uniform `update(t,x) -> score` contract: rule (3σ-rolling, IQR-rolling), statistical (EWMA control, two-sided CUSUM), ML (sliding-window IsolationForest, river `HalfSpaceTrees`, river `OneClassSVM`), sequence (PyTorch LSTM autoencoder with warm-up training and residual scoring). **Concept-drift gating** via ADWIN with `reset` + 60-step rebase cooldown — separates "score generator" from production alarm system. Alert pipeline: threshold ↦ cooldown ↦ grouping. Eval suite: **NAB scoring profile** (early-detection sigmoid, FP/FN penalties), standard P/R/F1, time-to-detect, **alert-budget curves** (precision vs alerts/day at the SRE manager's actual constraint), cost-aware operating point. Per-stream + portfolio champion picks. Production hygiene: per-event latency P50/P95/P99 (µs), state-checkpoint roundtrip parity, alert-stream JSONL audit, model card. CPU-only | 6-10 min |
| `graph_fraud_ring_detection.ipynb` | Graph / Anti-Fraud | Advanced | Node-level fraud classification + ring-level investigator queue on **PaySim** (Kaggle download, ~6.3M tx filtered to TRANSFER+CASH_OUT, 30-step window → ~57k accounts, ~35k edges, 1.13% fraud-node rate). Three-tier benchmark: hand-engineered graph features (degree / centrality / PageRank / clustering / reciprocity) + LightGBM, **Node2Vec** with hand-rolled biased random walks + gensim Word2Vec + LightGBM, hand-rolled **GraphSAGE** in plain PyTorch (no `torch_geometric` dependency, mean aggregator, 2 layers). Community detection (Louvain / Label Propagation / Connected Components) with fraud-purity scoring; embedding-cosine link prediction; **investigator queue** with precision@K under K=25 ring budget. Persisted artefacts with bit-identical inference parity, model card. CPU-only | 6-10 min |
| `contextual_bandits_offline_evaluation.ipynb` | Bandits / OPE | Advanced | Off-policy evaluation on the **Open Bandit Dataset** (ZOZOTOWN bundled sample; bypasses obp's broken `pre_process` via direct CSV load). Six candidate policies (uniform random, top-popular, IPW logistic regression, **LinUCB**, linear Thompson sampling, **neural Thompson** with bootstrap-head MLP) and four OPE estimators (DM with LightGBM q-hat, IPS, **SNIPS**, DR). Hybrid Saito-2020 methodology: train on BTS logs, OPE on BTS test split, benchmark against **ground-truth value** computed on the uniform-random log (1/80 propensity → unbiased oracle). Bootstrap CI on the policy ranking; PSI drift gate on context distribution; persisted policies + champion + qhat, model card with operating-point table. CPU-only | 1-2 min |
| `classic_nlp_text_classification_topics.ipynb` | NLP / Cost-Quality Benchmark | Advanced | Head-to-head classical-vs-LLM benchmark on **20 Newsgroups** (sklearn, headers/footers/quotes stripped). Five classifiers: TF-IDF + LinearSVM, TF-IDF + LogReg, LSA + LightGBM, MiniLM sentence-embeddings + LightGBM, **fine-tuned DistilBERT** (CPU subsample). Three topic-modelling approaches (LDA, NMF, BERTopic-style KMeans-on-embeddings + c-TF-IDF) scored against ground-truth labels via **NMI / homogeneity / completeness** + UMass coherence. Cost-quality leaderboard with macro-F1, train time, per-doc inference latency (µs), model size (MB); semantic-search demo; **explicit "use X when Y" decision memo** linking F1-gap × volume × CPU-hour to deployment recommendation. CPU-only | 10-15 min |
| `geo_experiments_hierarchical_bayesian.ipynb` | Causal / Bayesian Meta-Analysis | Advanced | Marketing-spend geo experiment on 30 simulated metro markets (104 weeks; 10 treated; planted heterogeneous lifts θ_i ~ N(0.05, 0.02)). Naive pre/post and DiD baselines, hand-rolled **Synthetic Control** with SLSQP-constrained donor weights and pre-period residual bootstrap CIs, **random-effects meta-analysis** via hand-rolled Metropolis-Hastings (2 chains × 6000 iter, R-hat / ESS, conjugate Gibbs step on θ_i; matches the SEIR / fisheries pattern, no PyMC dependency). Forest-plot shrinkage visualisation, posterior-predictive checks, MDE / power curve. Recovers ground-truth global mean within Bayesian credible interval; honest coverage benchmarking against truth. CPU-only | 5-8 min |
| `multi_label_text_classification_reuters.ipynb` | NLP / Multi-Label | Advanced | Multi-label tagging benchmark on **Reuters-21578** (NLTK ModApte split; 90 topics with severe head/tail imbalance). Six estimators: Binary Relevance + LinearSVM, Classifier Chains + LogReg, **Label Powerset** on top-20, OvR LightGBM, multi-label MLP (PyTorch + BCE + pos_weight), DistilBERT with `problem_type="multi_label_classification"` (sub-corpus + 1 epoch CPU). **Per-label F1-max threshold tuning** on validation, **per-label Platt calibration** with reliability diagrams, **asymmetric-cost operating points** (FN:FP cost ratios), comprehensive eval (Hamming, subset accuracy, micro/macro/sample F1, LRAP, coverage error), explicit **head-vs-tail breakdown** surfacing rare-label collapse. Persisted vectoriser + per-label models + per-label thresholds + per-label calibrators, inference parity. CPU-only | 8-12 min |
| `ner_sequence_labeling_wikiann.ipynb` | NLP / Sequence Labeling | Advanced | Token-level **NER** on WikiAnn-en (CoNLL-2003 substitute since HF datasets 4.0 deprecated loading scripts; 5k train / 2k val / 2k test sample, PER/ORG/LOC). Three-tier benchmark: linear-chain CRF (`sklearn-crfsuite`) with hand-crafted features, **BiLSTM-CRF** in plain PyTorch (hand-rolled forward-backward partition function + Viterbi decoding, no `allennlp` / `pytorch-crf` dependency), fine-tuned **DistilBERT** for token classification with subword-to-word alignment and BIO repair. **Entity-level seqeval F1** with per-type breakdown, error analysis (boundary vs type errors, type-confusion table), cost-quality Pareto. **Active-learning loop** on the CRF: random vs uncertainty (min-token-margin) sampling with F1-vs-budget curve. CPU-only | 5-8 min |
| `llm_agents_tool_use_evaluation.ipynb` | LLM Agents / Tool Use | Advanced | Self-contained synthetic **agent benchmark** with known ground truth (50 multi-step tasks across math+kb / multi_hop_kb / code_exec / mixed families). Tool harness: AST-evaluated calculator, TF-IDF KB search over 18 synthetic company documents, sandboxed `code_exec` (no imports, allow-listed builtins), date stub. Four agent strategies (zero-shot ReAct, few-shot ReAct, **structured-output / function-calling**, CoT-only) sharing a strategy-agnostic runner. Trace-level evaluation: task success rate, tool-call accuracy, plan validity, **failure-mode taxonomy** (tool hallucination, infinite loop, premature stop, JSON violation, wrong tool, wrong args), **cost-quality Pareto**. Deterministic FakeLLMClient with documented success / failure-mode parameter table; `USE_LIVE_API` flag swaps in real Anthropic / OpenAI client. Persisted benchmark + traces JSONL + model card. CPU-only | 1 min |
| `llm_dpo_preference_learning.ipynb` | LLM / Alignment | Advanced | End-to-end **Direct Preference Optimization** (Rafailov et al. 2023) on `distilgpt2`. Synthetic preference dataset (600 triplets) with sentiment-controlled completions and a frozen `distilbert-sst-2` **oracle judge** for honest win-rate evaluation. Three-stage pipeline: base → **SFT** on chosen completions (1 epoch) → **DPO** on full pairs (2 epochs, hand-rolled DPO loss with reference-model log-prob precomputation, no `trl` dependency). Eval: **reward-margin distributions** (base vs SFT vs DPO), **win-rate vs SFT** (oracle judge on 80 held-out generations), **KL divergence to reference**, qualitative side-by-side samples, **β sweep** producing the win-rate-vs-KL Pareto front. Persisted DPO/SFT weights, training history JSONL, model card with hyperparameters + Pareto + decision memo on when to ship DPO vs RLHF. CPU-only | 25-35 min |
| `summarization_extractive_vs_abstractive.ipynb` | NLP / Summarisation | Advanced | Single-document news summarisation cost-quality benchmark on **CNN/DailyMail v3.0.0** (150-article test sample, no auth). Five summarisers: **Lead-3**, **TextRank** (hand-rolled TF-IDF + PageRank), **BERT-Extractive** (MiniLM centroid scoring), fine-tuned **DistilBART-CNN** (`sshleifer/distilbart-cnn-12-6`), **T5-small** with `summarize:` prefix. Three metric families: **ROUGE-1/2/L**, **BERTScore-F1**, **faithfulness** via **lexical fidelity** + **NLI entailment** (`roberta-large-mnli`, scoped to abstractive systems × 50 articles for runtime). Cost-quality leaderboard with latency / model size / faithfulness; explicit Pareto plot showing where extractive wins on speed-and-faithfulness vs where abstractive wins on fluency. Decision memo with regulatory-industry stance (extractive when faithfulness is structural, abstractive when paraphrase compression is the goal). CPU-only | 25-35 min |
| `mlops_mlflow_drift_lifecycle.ipynb` | MLOps | Advanced | Full ML lifecycle on a synthetic credit-risk classifier with **planted drift at week 40** (income -15%, debt-to-income +30%, late_payments λ 0.6→1.0): 5-config LightGBM hyperparameter sweep tracked in **MLflow** (local SQLite backend, no external server), winner registered with **production alias**, weekly **batch scoring** loop with per-feature **PSI** drift detection, **automated retraining trigger** (per-feature PSI ≥ 0.25 OR mean PSI ≥ 0.10 for 2 consecutive batches), **A/B promotion gate** (candidate must beat production by ≥ 0.01 AUC on labelled holdout). Full lifecycle audit with served-version-per-week, retrain events, lift-vs-baseline. Decomposes drift detection / concept-drift monitoring / A/B comparison / promotion as independent layers; explicit decision memo on threshold tuning. CPU-only | 4-6 min |
| `marketing_spend_allocation_optimization.ipynb` | Optimization / Marketing | Advanced | Channel-budget allocation across 6 paid-media channels (synthetic 156 weeks × 6 channels with known ground-truth Hill saturation parameters). Per-channel **Hill curve fit** (`α s^β / (γ^β + s^β)`) via NLLS with **bootstrap parameter CIs**; parameter-recovery diagnostic against ground truth. **Convex optimization** in `cvxpy`: piecewise-linear concave envelope of the fitted Hill, total-budget + per-channel-floor/ceiling constraints, solved in milliseconds via SCS. Comparison: optimal vs historical vs equal-split vs proportional-to-effective-CPM. **Bootstrap-mean robust allocation** vs point-estimate optimum. **Sensitivity sweep**: efficient frontier (revenue vs budget) + per-channel optimal spend curves. **Marginal-ROAS-at-optimum** table (KKT-equality check across unconstrained channels). CPU-only | 1-2 min |
| `inventory_multi_echelon_supply_chain.ipynb` | Operations Research / Supply Chain | Advanced | Single-SKU 3-echelon supply chain (manufacturer → DC → store) with stochastic demand and base-stock policies. **Newsvendor** closed-form `F^{-1}(C_u/(C_u+C_o))` validated by Monte Carlo. **Base-stock** multi-period simulation tracking holding/stockout cost + fill rate. **Clark-Scarf** (1960) per-echelon decomposition with declining service-level targets upstream. **Bullwhip-effect** quantification: smart vs naive policy comparison with variance-amplification ratios. **Service-level vs cost frontier** (β sweep 0.85→0.99). Demand-distribution sensitivity (Gaussian vs Poisson vs lognormal — when closed-form Gaussian over- or under-stocks). **Policy comparison** base-stock vs (Q,r) continuous-review vs constant-order. Decision memo with extensions for fixed ordering cost ((s,S) policies), forecast-driven non-iid demand, stochastic lead times. CPU-only | 1-2 min |
| `workforce_shift_scheduling_mip.ipynb` | Operations Research / Workforce | Advanced | Weekly shift roster optimization for a single retail / call-center / hospital site (15 workers × 21 shifts = 7 days × 3, 4 skills, 12-hour minimum rest gap, per-worker hours cap, availability matrix). Mixed-integer program in **PuLP + CBC** (no commercial solver needed): binary `x[w,s]` decision variables, coverage / skill / hours-cap / rest-gap / availability constraints, total-cost-minimization objective. Greedy most-constrained-first heuristic baseline; MIP saves 10-20% on this instance. Solution visualization (worker × shift heatmap + per-skill coverage bar plot). Sensitivity sweep: tightened coverage, dropping cheapest worker, relaxing rest-gap (shadow price of regulation). **Robust extension**: over-cover buffer β to absorb stochastic no-shows, with cost-vs-resilience trade-off curve. CPU-only | <1 min |
| `vehicle_routing_clarke_wright_2opt.ipynb` | Operations Research / Routing | Advanced | Capacitated Vehicle Routing Problem (**CVRP**) on a synthetic 1-depot + 40-customer instance with vehicle capacity 50 and Euclidean distances. Four-stage solution stack: **TSP baseline** (nearest-neighbor + 2-opt single tour, ignoring capacity — the lower bound), **Clarke-Wright savings** (1964) hand-rolled greedy CVRP construction, **2-opt** intra-route local search, **simulated annealing** with relocate / swap inter-route operators (escapes 2-opt local optima). Visual route maps (4-panel: TSP / savings / +2-opt / +SA), capacity-sensitivity sweep (fleet size vs total distance Pareto), **VRPTW extension** with morning/afternoon/evening time windows + service times. Decision memo on when each algorithm wins, when to call commercial solvers, road-network distance integration. CPU-only | 1-2 min |
| `llm_reasoning_evaluation_gsm8k.ipynb` | LLM / Reasoning | Advanced | Math-reasoning benchmark on **GSM8K** (60-problem subsample of the 1,319-problem test split, public on HF). Three prompting strategies on **Qwen2.5-0.5B-Instruct**: **no-CoT** (direct answer), **chain-of-thought** ("let's think step by step"), **self-consistency** (sample N=3 CoT chains with T=0.7, majority-vote the numeric answer). Eval: exact-match accuracy, mean tokens per problem, mean wall-clock latency, **CoT lift** (CoT − no-CoT), **SC lift** (SC − CoT). Self-consistency vote-pattern analysis (3-of-3 correct vs 2-of-3 vs split). Cost-quality Pareto. Decision memo on when CoT pays, when SC pays, when neither does. Completes the LLM-eval triplet (RAG-eval ✓ → agents-eval ✓ → reasoning-eval). CPU-only | 30-60 min |
| `fairness_robustness_deep_dive.ipynb` | ML Ethics / Robustness | Advanced | Multi-method fairness audit + robustness stress test on the **UCI Adult Census Income** dataset (sex as sensitive). Six fairness metrics computed per group (selection rate, TPR, FPR, PPV, AUC, Brier) at threshold 0.5 + per-group calibration plots. Two **mitigations** with cost-quality comparison: **Reweighing** (Kamiran & Calders 2012, pre-processing sample weights) and **Adversarial debiasing** (Zhang et al. 2018, hand-rolled gradient-reversal layer in PyTorch). **Counterfactual fairness** flip diagnostic (mean absolute prediction change when sensitive attribute is flipped). **Robustness stress test**: Gaussian feature noise, random feature dropout, FGSM-style adversarial perturbation. **Subgroup decision-curve analysis** (net benefit per group at varying thresholds). Model card v2 with per-subgroup operating points, mitigation tier, robustness profile, decision memo. CPU-only | 3-5 min |
| `embedding_finetuning_contrastive_hard_negatives.ipynb` | NLP / Representation Learning | Advanced | Domain-adaptive sentence-embedding fine-tuning via **Multiple Negatives Ranking Loss** (MNRL) with **hard-negative mining** on **SNLI triplets** (entailment = positive, contradiction = hard negative). 6,000 triplets subsample; hand-rolled MNRL training loop in PyTorch (no `sentence-transformers.SentenceTransformerTrainer` dependency); MiniLM-L6 base encoder fine-tuned for 1 epoch. **Three-task head-to-head** vs base MiniLM: **STS-Benchmark** Spearman correlation, **retrieval top-1 / top-5** accuracy on STS-B-derived (query, doc) pairs, **20 Newsgroups clustering** NMI. Lift table + decision memo on when fine-tuning pays vs swapping to a larger pretrained encoder. Drop-in replacement for the base encoder in `genai_rag_pipeline`. CPU-only | 25-40 min |
| `topological_data_analysis_persistent_homology.ipynb` | Topology / Geometry | Advanced | **Persistent homology** for shape classification — completely different mathematical foundation. **Synthetic** four-class dataset (sphere / torus / two-spheres / swiss-roll, 80 instances each, 200 points) with known ground-truth Betti numbers + **MNIST as 2D point clouds** (1,000 digits, 100 per class). Vietoris-Rips persistence diagrams via `ripser` up to maxdim=2; three vectorisation strategies head-to-head: **statistical summaries** (15-dim), **Betti curves** (60-dim), **persistence images** (Adams et al. 2017, ~600-dim via `persim`). Naive baselines (coordinate moments, pairwise-distance histograms) prove TDA is doing real work. LightGBM classifier on each representation; full leaderboard with accuracy / F1 / train time / per-sample inference latency. Decision memo on when TDA wins (rotation-invariant labels, small data, time-series-as-attractor), when it loses (large rich data + CNN). CPU-only | 1-2 min |

## GenAI stack — how the four notebooks compose

The four `genai_*` / `llm_rag_*` notebooks share a single contract (`Prediction` → `EvalReport` → release gate) and run in sequence:

```text
genai_dataops_vector_platform  →  genai_rag_pipeline  →  llm_rag_evaluation  →  genai_service_delivery
            (optional)              predictions.jsonl     eval_report.json      release_decision.json
```

All four import the same primitives from [`src/genai/`](../src/genai/): `Document` / `Chunk` / `Prediction` schemas, `SentenceTransformerEmbedder`, `DenseRetriever` / `BM25Retriever` / `HybridRetriever` / `CrossEncoderReranker`, `InputGuardrail` / `OutputGuardrail`, `RAGPipeline`, `Tracer` / `CostTracker`, and the RAGAS-style metrics in `src/genai/evals.py`. Toggling `USE_LIVE_API = True` at the top of each notebook swaps the deterministic `FakeLLMClient` for real Anthropic or OpenAI calls without any other changes.

Running the full end-to-end takes ~2 minutes on a CPU-only laptop; CI / offline runs are reproducible because the fake LLM's outputs are deterministic on input.

## Notes

- Most notebooks are self-contained and use synthetic data.
- For `healthcare_analysis.ipynb`, prefer the pinned environment from the repository root `README.md` (`requirements-notebook-healthcare-shap.txt`).
- To execute a notebook non-interactively:

```bash
jupyter nbconvert --to notebook --execute --inplace notebooks/<notebook_name>.ipynb
```
