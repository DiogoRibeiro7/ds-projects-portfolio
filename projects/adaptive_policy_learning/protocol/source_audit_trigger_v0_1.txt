Adaptive Policy Learning official Open Bandit Dataset Study 2 corrected primary OPE trigger v2.2

Purpose: execute the single corrected Study 2 primary off-policy evaluation authorized by study2_primary_ope_execution_erratum_v1_2.json after the implementation-only pandas timestamp-resolution failure in workflow run 33807198572.

Pre-evaluation authorization: the runner must refit the frozen BTS training partition with newton-cholesky L2 logistic regression (l1_ratio=0.0, C=1.0, tol=0.0001, max_iter=100) and reproduce exactly 73 features, n_iter=6, zero optimizer warnings, the locked scientific runtime, and coefficient SHA256 7438db24286013c628dc7f74e2dd7f4913cdd05297c717fff78a214c9afb5684. Any mismatch aborts before evaluation outcomes are read.

Erratum-only execution correction: validate the frozen BTS evaluation row count and first/last timestamps using timestamp and position columns only, use the shared Study 2 evaluation-slice contract, and convert parsed evaluation timestamps explicitly to nanosecond resolution before integer comparison/storage. These changes are implementation compatibility corrections only and do not alter any scientific parameter or estimator.

Primary analysis authorization: only after the qualified model and outcome-free temporal boundary checks pass, read the frozen BTS evaluation window and exact matched Random reference window and compute the preregistered uniform benchmark and epsilon=0.10 challenger using IPS, SNIPS, DM, and DR; primary unclipped weights with clipping sensitivities 5/10/20; ESS and overlap diagnostics; paired 24-hour moving-block bootstrap with 1999 replications and seed 20260831; and the frozen promotion rule requiring the lower 95% bootstrap confidence bound for challenger DR minus observed BTS value to exceed zero and challenger ESS/n >= 0.10.

Scientific boundary: this trigger authorizes exactly one corrected Study 2 primary OPE rerun under protocol version 1.2-study2-primary-ope-execution-erratum. No solver, regularization, preprocessing, feature, split, support, epsilon, estimator, clipping, bootstrap, or promotion-rule change is authorized. No secondary analysis is authorized before this primary rerun completes. The workflow and runner must preserve a v1.2 result artifact on success, handled failure, prerequisite failure, interruption, or timeout.
