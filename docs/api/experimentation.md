# Experimentation Stack

```{note}
The modern-bank-churn modules currently contain syntax errors (see
`docs/DOC_COVERAGE.md`), so their API reference cannot be generated until the
source files are fixed. Once the `enhanced_feature_engineering.py` parser issue
is resolved, the automated docs will include the ML pipeline orchestrator,
feature engineering, modeling, evaluation, and production guardrail modules.
```
