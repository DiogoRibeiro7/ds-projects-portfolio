# Quality Report Archive

This directory stores historical, point-in-time quality outputs that are useful
for reference but should not live at the repository root.

Examples:

- `mypy_output*.txt`

Do not add runtime-generated files here automatically from CI. Use this archive
only when intentionally preserving a snapshot.
