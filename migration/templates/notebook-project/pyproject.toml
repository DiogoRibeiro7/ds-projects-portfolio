[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "notebook-project"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["jupyter", "nbformat"]

[project.optional-dependencies]
test = ["pytest", "ruff"]
