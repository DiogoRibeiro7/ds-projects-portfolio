# SHAP-compatible environment for notebooks/healthcare_analysis.ipynb
#
# This intentionally does NOT include `-r requirements.txt` because the full
# repo stack contains broad dependencies that can conflict with SHAP/Numba.
# Use this as a dedicated notebook-only environment.

# Core notebook + ML stack
numpy>=2.2.0,<2.3.0
pandas>=2.2.0
scipy>=1.14.0
matplotlib>=3.9.0
seaborn>=0.13.0
scikit-learn>=1.6.0,<1.8

# SHAP dependency chain (Numba-compatible)
numba>=0.61.0,<0.62.0
llvmlite>=0.44.0,<0.45.0
shap>=0.46.0,<0.47.0

# Notebook runtime
jupyterlab>=4.0.0
ipykernel>=6.29.0
